import type { Options } from "prettier";

const config: Options = {
  printWidth: 120,
  tabWidth: 2,
  useTabs: false,
  semi: true,
  singleQuote: true,
  trailingComma: "all",
  bracketSpacing: false,
  arrowParens: "always",
  endOfLine: "lf",
};

export default config;
